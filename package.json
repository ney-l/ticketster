{
  "scripts": {
    "dev": "skaffold dev",
    "prepare": "husky install",
    "update:common-in-all": "cd auth && npm i @ticketster/common && cd ../orders && npm i @ticketster/common && cd ../tickets && npm i @ticketster/common"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "mongoose-update-if-current": "^1.4.0"
  }
}
